labels:
  defaults:
  - defaultLabel
  matching_paths:
  - paths:
    - path1/*
    values:
    - path1
  - paths:
    - .kogibot-config.yml
    values:
    - kogibot-config
  - paths:
    - existing-default-label
    values:
    - 'needs review :mag:'