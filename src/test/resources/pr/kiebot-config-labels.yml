labels:
  defaults:
  - defaultLabel
  matching_paths:
  - paths:
    - path1/*
    values:
    - path1
  - paths:
    - kiebot-config.yml
    values:
    - kiebot-config
  - paths:
    - existing-default-label
    values:
    - 'needs review :mag:'